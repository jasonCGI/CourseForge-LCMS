"""OAM (Offline Asset Module) importer.

Extracts an uploaded OAM package, reads its manifest, and records an OamAsset
with its file tree and capability flags. Implemented in a later sprint.
"""


def import_oam(media_asset, archive_path):
    """Extract and register an OAM package. Stub — implemented later."""
    raise NotImplementedError("import_oam is implemented in a later sprint")
