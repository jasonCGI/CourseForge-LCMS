# Ecosystem tool URLs for the header tray (Vite — baked in at build time).
# Copy to .env.local for dev, or set on the Railway CourseForge service for prod.
VITE_FORGEBLUEPRINT_URL=
VITE_FORGEPACK_URL=
VITE_FORGECLIP_URL=
